import React from 'react';
import CartScreen from '../../cart/cartScreen';
import {
    SafeAreaView
} from 'react-native';

const ModalShoppingCart = ({ navigation }) => (

    <CartScreen navigation={navigation} />

)
export default ModalShoppingCart;